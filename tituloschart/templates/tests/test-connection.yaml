apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tituloschart.fullname" . }}-test-connection"
  labels:
    {{- include "tituloschart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tituloschart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
